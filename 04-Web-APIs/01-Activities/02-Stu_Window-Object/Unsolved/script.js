// TODO: What will the following line of code log?
// the window properties
console.log(window);

// TODO: What will the following line of code log?
//the html properties?
console.log(window.document);

// TODO: What will the following line of code log?
// all the elements in the html file? or maybe the first element? so the meta data? or maybe the html tag since it's the first one?
console.log(document.documentElement);

// TODO: What will the following line of code log?
// the content of the head element which is the title which is "Window Object"?
console.log(document.head);
console.log(Object.keys(window));
console.log(Object.keys(window).length);

